{
  "name": "order-webhook",
  "version": "1.0.0",
  "description": "Checks recent orders and sends a webhook if a new order is in",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.6",
    "express": "^4.17.2",
    "fs": "^0.0.1-security",
    "googleapis": "^94.0.0",
    "jfile": "^1.1.12",
    "node-fetch": "^3.2.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
